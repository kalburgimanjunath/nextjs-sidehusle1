import Navbar from './navbar';
export default function Header() {
  return (
    <div
      style={{
        // display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Navbar />
    </div>
  );
}
